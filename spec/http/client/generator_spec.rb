# frozen_string_literal: true

RSpec.describe Http::Client::Generator do
  it "has a version number" do
    expect(Http::Client::Generator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
